export const ATTR_ROOT = "torph-root";
export const ATTR_ITEM = "torph-item";
export const ATTR_ID = "torph-id";
export const ATTR_EXITING = "torph-exiting";
export const ATTR_DEBUG = "torph-debug";

/**
 * ID of the placeholder segment that stands in for an emptied value. It holds
 * a zero-width space so the container keeps a line box while the exits play,
 * and is excluded from measurement, animation and exit handling.
 */
export const EMPTY_ID = "empty";
